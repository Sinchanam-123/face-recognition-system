# Web backend
flask>=3.0
flask-cors>=4.0

# Recognition stack — installs cleanly on Python 3.14 (no dlib needed)
insightface>=0.7.3        # ArcFace detection + 512-d embeddings
onnxruntime>=1.20         # runs the ONNX models on CPU
opencv-python>=4.9        # webcam capture + drawing
numpy
pandas
Pillow

# First time you start the camera, InsightFace downloads the 'buffalo_sc' model
# pack (~15 MB) to ~/.insightface/models — this needs an internet connection once.
